// collect/static — file discovery, ESLint/a11y-plugin runner, inventories.
// Emits StaticArtifacts; imports record only (+ its own heavy lint deps).
// Lands in M1. This module is the `./collect-static` subpath export.

export const COLLECT_STATIC_MILESTONE = 'M1' as const;
